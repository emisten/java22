package se.iths.emilius.exercises.chapter6.vehicledealer;

import java.math.BigDecimal;

public class Car extends Land {
    public Car(int weight, BigDecimal price, Person owner ) {
        super(weight, price, owner);
    }
}
